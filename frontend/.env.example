# 아래 값을 비워두면 기본값을 그대로 쓴다(로컬 개발: 백엔드 http://localhost:8000).

# VITE_API_BASE_URL=/api
# VITE_API_TARGET=http://localhost:8000

# 공유 API 키(선택). 백엔드에 API_KEY를 설정했다면(루트 .env.example 참고) 반드시
# 여기도 같은 값으로 맞춰야 한다 — 안 맞으면 모든 API 요청이 401로 실패한다.
# VITE_API_KEY=
